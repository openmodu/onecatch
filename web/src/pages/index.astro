---
import Base from "../layouts/Base.astro";
import RunStream from "../components/RunStream.astro";
import Section from "../components/Section.astro";
import Workbench from "../components/Workbench.astro";

const title = "OneCatch";
const description =
  "OneCatch — One place, catch everything。在本地驱动你已经装好的 Codex 和 Claude Code，把「实现 → 审查 → 返工」写成一张能停、能续的状态机。";
---

<Base title={title} description={description}>
  <div class="column">
    <section class="hero" id="top">
    <span class="chip"><span class="dot" aria-hidden="true"></span>本地运行 · 数据在 <code>~/.onecatch</code></span>
    <h1>把你的工作方法，<br class="wide-break" />写成 Agent 能跑的 loop。</h1>
    <p class="lede">OneCatch 在本地驱动你已经装好的 Codex 和 Claude Code。「实现 → 审查 → 返工」不再是你每次手动重复的动作，而是一张有信号、有上限、中途能插话也能接着跑的状态机。</p>
    <div class="actions">
      <a class="button primary" href="#build">从源码构建</a>
      <a class="button ghost" href="#timeline">先看它怎么跑</a>
    </div>
    <div class="drives">
      <span class="label">驱动你已经在用的 CLI</span>
      <span><span class="cli">codex</span> · <span class="cli">claude</span> · <span class="cli">modu</span></span>
    </div></section>
  </div>

  <RunStream />

  <div class="column">
    <Section id="timeline" eyebrow="能力" title="让 Agent 的每一步都看得见、停得下">
      <div class="features">
        <div class="feature">
          <h2>换 Agent，不用换读法</h2>
          <p>Codex 走 JSON-RPC app-server，Claude Code 走 <code>--output-format stream-json</code>，两边吐出来的东西格式完全不同。OneCatch 把它们归一成同一组事件：<code>started</code>、<code>reasoning</code>、<code>tool_use</code>、<code>tool_result</code>、<code>file_change</code>、<code>usage</code>、<code>result</code>。你在界面上看到的、写进 run log 的、决定 workflow 往哪走的，是同一份数据。</p>
        </div>

        <div class="feature">
          <h2>停得下来，也接得回去</h2>
          <p>跑的过程中可以排队追加指令，也可以打断当前这一轮直接插话。暂停之后按 session ID 恢复——Claude Code 是 <code>--resume</code>，Codex 是 <code>thread/resume</code>——事件继续追加到同一条 run log，不会另起一条。</p>
        </div>

        <div class="feature">
          <h2>权限写在每一步上，不是一个全局开关</h2>
          <p><code>read-only</code>、<code>workspace-write</code>、<code>full</code> 三档，是 workflow 里每个 step 各自的属性。实现那一步可写，审查那一步只读，这是最常见的配法。<code>full</code> 要过一次二次确认，而且不允许跑在远端 worker 上。</p>
        </div>

        <div class="feature">
          <h2>写错的 loop 会自己停</h2>
          <p>每个 step 声明它可能输出哪些信号，以及每个信号跳到哪一步。默认最多流转 20 次，连续失败到阈值也停。所以一个绕不出去的循环撞的是上限，不是你一整晚的 token。</p>
        </div>

        <div class="feature">
          <h2>数据就是你磁盘上的文件</h2>
          <p>JSON / JSONL 存在 <code>~/.onecatch</code>，每次运行的事件流在 <code>~/.onecatch/runs/</code>。可以 grep，可以进 git，可以直接删。没有账号，没有遥测，没有一个云端服务夹在你和 Agent 中间。</p>
        </div>

        <div class="feature">
          <h2>把 Linux 目录当本地目录用</h2>
          <p><code>onecatchfs</code> 用一条常驻的 SSH/SFTP 连接，把 Linux 上的项目挂成 macOS 本地目录。文件内容不落到 Mac 上，只缓存属性和目录列表，默认 1 秒。Linux 那头只要跑着 sshd——不用装 Codex，也不用装 OneCatch。</p>
        </div>
      </div>
    </Section>

    <Section id="ui" eyebrow="界面" title="一个任务的全部上下文，在同一屏里" />
  </div>

  <Workbench />

  <div class="column">
    <section class="section" id="build">
    <p class="eyebrow">构建</p>
    <h2>还没有发布二进制，先自己编</h2>
    <pre class="code"><span class="prompt">$</span> git clone git@github.com:openmodu/onecatch.git
    <span class="prompt">$</span> cd onecatch
    <span class="prompt">$</span> go tool wails3 task deps            <span class="note"># 按 lockfile 装 Go 和 JS 依赖</span>
    <span class="prompt">$</span> go tool wails3 task build:desktop    <span class="note"># 输出桌面开发版本</span></pre>
    <p class="note-line">需要 Go 和 Node，版本以仓库里的 <code>go.mod</code> 和 <code>frontend/package.json</code> 为准。想把 Linux 目录挂进来的话，另外装 macFUSE，再跑 <code>go tool wails3 task build:onecatchfs</code>（只支持 macOS）。开发环境是 <code>go tool wails3 task dev:desktop</code>。</p>
    </section>

    <section class="section" id="nongoals">
    <p class="eyebrow">不做什么</p>
    <h2>这些事 OneCatch 不打算做</h2>
    <div class="steps">
      <p><strong>不托管代码，也不替你跑 Agent。</strong>它驱动的是你本机上已经装好的 CLI，用的是那些 CLI 自己的登录和 API key。OneCatch 不转发你的请求，也就拿不到你的 key。</p>
      <p><strong>不做账号和云同步。</strong>换一台机器就是换一份 <code>~/.onecatch</code>，同步得你自己想办法。这是本地优先的代价，写在这里而不是等你装完才发现。</p>
      <p><strong>仓库里的市场、登录、计费代码不是产品。</strong>那是早期探索留下的，当历史参考保着，不约束现在的本地编排实现。</p>
    </div>
    </section>

    <Section id="questions" eyebrow="问题" title="你大概会问的几件事">
      <div class="qa">
        <details>
          <summary>和自己开几个终端跑 CLI 有什么区别？</summary>
          <p>区别在「跑完之后」。终端里 Agent 停下来，你得自己判断这轮结果算不算过、要不要返工、返工时把哪些上下文再喂一遍。OneCatch 把这个判断写成 step 的输出信号和跳转表，所以「审查没过就回到实现」是它自己完成的。附带的好处是每一轮的事件、token、改了哪些文件都落在同一条 run log 上，不用回滚终端 scrollback 找。</p>
        </details>
        <details>
          <summary>要额外配 API key 吗？</summary>
          <p>不要。OneCatch 起的是你机器上的 <code>codex</code> / <code>claude</code> 可执行文件，认证走它们自己那一套，请求也不经过 OneCatch——所以它既拿不到你的 key，也没法替你计费。</p>
        </details>
        <details>
          <summary>能选模型和推理档位吗？</summary>
          <p>能，而且是每个 step 单独选。step 上可以指定 runtime、模型、reasoning effort 和 service tier；没指定的走全局默认。这样「实现」用贵一点的模型、「审查」用便宜的，是配置问题不是改代码问题。</p>
        </details>
        <details>
          <summary>支持 Windows 和 Linux 吗？</summary>
          <p>macOS 和 Windows 各有自己的窗口与菜单实现，也各有打包脚本——Windows 走无边框标题栏和原生菜单栏，macOS 走 inset 标题栏。Linux 没有打包目标，Wails 本身支持，但仓库里没验证过。<code>onecatchfs</code> 只有 macOS（依赖 macFUSE）；远端 worker 有 macOS 和 Linux 两个构建目标。</p>
        </details>
        <details>
          <summary>可以让它在另一台机器上跑吗？</summary>
          <p>两条路。一是远端 worker：把 step 指到某个 worker 上执行，但 <code>full</code> 权限的 step 不允许这么跑。二是 <code>onecatchfs</code>：把 Linux 目录挂成 macOS 本地目录，Agent 还是在本机跑，只是文件在远端。</p>
        </details>
        <details>
          <summary>我的数据会被传到哪里去吗？</summary>
          <p>不会。没有账号系统，没有遥测，没有 OneCatch 的服务端。任务、运行记录、事件流都是 <code>~/.onecatch</code> 下的 JSON / JSONL 文件。Agent 本身当然要连它自己的模型服务，那部分和你直接用 CLI 时完全一样。</p>
        </details>
      </div>
    </Section>
  </div>
</Base>

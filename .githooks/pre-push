#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if ! command -v go >/dev/null 2>&1; then
    echo "pre-push: Go is required to run the test suite" >&2
    exit 1
fi

echo "pre-push: running Go tests"
go test ./...
echo "pre-push: passed"

import { useTranslation } from "react-i18next";
import { Action, StatusBadge } from "../../ui/primitives.jsx";

export default function WorkerPage({ workers, health, checkWorker, deleteWorker, openWorker }) {
  const { t } = useTranslation();
  return <div className="library-page worker-page"><div className="library-hero"><div><span className="kicker">{t("worker.kicker")}</span><h2>{t("worker.title")}</h2><p>{t("worker.description")}</p></div><Action tone="primary" onClick={() => openWorker(null)}>{t("worker.register")}</Action></div><div className="worker-grid">{workers.map((worker) => <article className="worker-card panel" key={worker.id}><div className="worker-card-head"><div className="worker-machine"><span><strong>{worker.name}</strong><small>{worker.id}</small></span></div><StatusBadge status={worker.enabled ? "completed" : "cancelled"} className="status-pill">{worker.enabled ? t("common.enabled") : t("common.disabled")}</StatusBadge></div><code>{worker.baseUrl}</code><div className="worker-health">{health[worker.id]?.checking ? t("worker.checking") : health[worker.id]?.ok ? <>{Object.entries(health[worker.id].runtimes || {}).map(([runtime, ok]) => <span key={runtime} className={ok ? "ok" : "missing"}><i />{runtime}</span>)}</> : health[worker.id]?.error || t("worker.notChecked")}</div><div className="worker-actions"><Action onClick={() => checkWorker(worker)}>{t("worker.health")}</Action><Action onClick={() => openWorker(worker)}>{t("common.edit")}</Action><Action tone="danger" onClick={() => deleteWorker(worker.id)}>{t("common.delete")}</Action></div></article>)}{!workers.length && <div className="empty-state"><h3>{t("worker.empty")}</h3><p>{t("worker.emptyDescription")}</p></div>}</div><div className="worker-command panel"><span className="kicker">{t("worker.command")}</span><code>ONESHOT_WORKER_TOKEN=... oneshot-worker --listen 0.0.0.0:9231 --id mac-mini --workspace workspace-id=/path/to/clone</code><p>{t("worker.networkWarning")}</p></div></div>;
}
